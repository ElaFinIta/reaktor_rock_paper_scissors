import React from 'react';

const Header = () => {
    return (
        <header className="App-header">
            <h1>Rock-paper-scissors match results</h1>
        </header>
    );
};

export default Header;